"""Core document processing module — PDF and QR utilities, pure (no Flet).

processor.py: merge, split, compress, rotate, watermark, stamp, encrypt.
converter.py: pdf_to_images, images_to_pdf, extract_text.
ocr.py: hybrid native-text/OCR extraction for scanned PDFs (extra [ocr]).
info.py: metadata + thumbnail extraction (get_pdf_info, render_first_page_png).
qr.py: QR code generation.
"""
